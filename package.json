{
  "name": "maharani-smart-store",
  "private": true,
  "version": "1.0.0",
  "workspaces": ["client", "admin", "server"],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\" \"npm run dev -w admin\"",
    "build": "npm run build -w server && npm run build -w client && npm run build -w admin",
    "seed": "npm run seed -w server"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
